
@{
    ViewData["Title"] = "Timetables";
    Layout = "~/Areas/Registrar/Views/Shared/_Layout.cshtml";

    int counter = 0;
}

<h4>@ViewData["Title"]</h4>

<p>
    <a asp-action="Create" class="btn btn-sm btn-flat btn-outline-danger">Create New Time Table</a>
    <a asp-action="AutomaticTimeTableGenerator" class="btn btn-sm btn-flat btn-outline-dark"> Timetable Generator</a>
</p>
<br/>
<form asp-action="PrintTimetable" method="post">
    <div class="row">
        <div class="col-lg-3">
            <label class="control-label">Class</label>
            <select name="ClassId" id="ClassId2" class="form-control" asp-items="ViewBag.Classes"><option value="-1">Select</option></select>
        </div>
        <div class="col-lg-3">
            <label class="control-label">Section</label>
            <select name="SectionId" id="SectionId" class="form-control"></select>
        </div>
        <div class="col-lg-3">
            <label class="control-label">Report Type</label>
            <select name="ReportType" class="form-control">
                <option value="STANDARD" selected="selected">STANDARD</option>
                <option value="CUSTOM">CUSTOM</option>
                <option value="TEACHER">TEACHER'S</option>
            </select>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-3">
            <label>&nbp;</label><br />
            <input type="submit" value="View Time Table" id="btn_submit" class="btn btn-md btn-danger" />
        </div>
    </div><
        
    div class="row">
        <div class="col-lg-12">&nbsp;</div>
    </div>
</form>
