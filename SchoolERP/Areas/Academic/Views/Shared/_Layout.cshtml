@inject IHttpContextAccessor _httpContextAccessor;
@{
    Layout = "~/Views/Shared/_MasterLayout.cshtml";
}

<!-- Main Sidebar Container -->
<aside class="main-sidebar elevation-4 sidebar-light-danger">
    <!-- Brand Logo -->
    <a href="/" class="brand-link bg-danger">
        <img src="~/dist/img/AdminLTELogo.png" alt="SchoolERP" class="brand-image img-circle elevation-3" style="opacity: .8">
        <span class="brand-text font-weight-light">SchoolERP</span>
    </a>

    <!-- Sidebar -->
    <div class="sidebar os-host os-host-resize-disabled os-host-transition os-host-overflow os-host-overflow-y os-host-scrollbar-horizontal-hidden os-theme-dark">
       
        @Html.Partial("_CurrentUserPanel")

        <!-- SidebarSearch Form -->
        <div class="form-inline">
            <div class="input-group" data-widget="sidebar-search">
                <input class="form-control form-control-sidebar" type="search" placeholder="Search" aria-label="Search">
                <div class="input-group-append">
                    <button class="btn btn-sidebar">
                        <i class="fas fa-search fa-fw"></i>
                    </button>
                </div>
            </div>
        </div>

        <!-- Sidebar Menu -->
        <nav class="mt-2">
            <ul class="nav nav-pills nav-sidebar flex-column nav-compact nav-child-indent" data-widget="treeview" role="menu" data-accordion="false">
                <!-- Add icons to the links using the .nav-icon class with font-awesome or any other icon font library -->
                <li class="nav-item">
                    <a href="#" class="nav-link active">
                        <i class="nav-icon fas fa-th"></i>
                        <p>
                            Lookups
                            <i class="right fas fa-angle-left"></i>
                        </p>
                    </a>
                    <ul class="nav nav-treeview">
                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "Rooms", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="Rooms" asp-action="Index" class="nav-link">
                                    <i class="far fa-circle nav-icon"></i>
                                    <p>Rooms</p>
                                </a>
                            </li>
                        }
                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "Semesters", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="Semesters" asp-action="Index" class="nav-link">
                                    <i class="far fa-circle nav-icon"></i>
                                    <p>Semesters</p>
                                </a>
                            </li>
                        }
                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "GradingRules", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="GradingRules" asp-action="Index" class="nav-link">
                                    <i class="far fa-circle nav-icon"></i>
                                    <p>Grading Rules</p>
                                </a>
                            </li>
                        }
                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "GradingRuleGroups", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="GradingRuleGroups" asp-action="Index" class="nav-link">
                                    <i class="far fa-circle nav-icon"></i>
                                    <p>Grading Rule Groups</p>
                                </a>
                            </li>
                        }
                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "Classes", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="Classes" asp-action="Index" class="nav-link">
                                <i class="far fa-circle nav-icon"></i>
                                <p>Classes</p>
                            </a>
                        </li>}
                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "Sections", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="Sections" asp-action="Index" class="nav-link">
                                <i class="far fa-circle nav-icon"></i>
                                <p>Sections</p>
                            </a>
                        </li>}
                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "Subjects", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="Subjects" asp-action="Index" class="nav-link">
                                <i class="far fa-circle nav-icon"></i>
                                <p>Subjects</p>
                            </a>
                        </li>}
                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "SubjectGroups", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="SubjectGroups" asp-action="Index" class="nav-link">
                                <i class="far fa-circle nav-icon"></i>
                                <p>Subject Groups</p>
                            </a>
                        </li>
                        }

                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "Sessions", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="Sessions" asp-action="Index" class="nav-link">
                                    <i class="far fa-circle nav-icon"></i>
                                    <p>Sessions</p>
                                </a>
                            </li>
                        }
                    </ul>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <i class="nav-icon fas fa-th"></i>
                        <p>
                            Academic Years
                            <i class="right fas fa-angle-left"></i>
                        </p>
                    </a>
                    <ul class="nav nav-treeview">
                        @if (Common.isAuthorized(Convert.ToInt32(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserRoleId)), Common.GetModuleText(Common.ModuleName.ACADEMIC), "AcademicYears", "Index"))
                        {
                            <li class="nav-item">
                                <a asp-controller="AcademicYears" asp-action="Index" class="nav-link">
                                    <i class="far fa-circle nav-icon"></i>
                                    <p>Academic Years</p>
                                </a>
                            </li>
                        }
                    </ul>
                </li>
            </ul>
        </nav>
        <!-- /.sidebar-menu -->
    </div>
    <!-- /.sidebar -->
</aside>

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1 class="m-0">Academic</h1>
                </div><!-- /.col -->
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="#">Academic</a></li>
                        <li class="breadcrumb-item active">Home</li>
                    </ol>
                </div><!-- /.col -->
            </div><!-- /.row -->
        </div><!-- /.container-fluid -->
    </div>
    <!-- /.content-header -->
    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
            <!-- CONTAINER -->
            <div class="card card-outline card-danger" style="padding-left:10px;padding-right:10px;">
                <div class="card-header">
                    <h3 class="card-title">Welcome: <span class="badge badge-info">@Html.Raw(_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyUserEmail))</span></h3>
                </div>
                <!-- /.card-header -->
                <div class="card-body p-0">
                    @RenderBody()
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div><!--/. container-fluid -->
    </section>
    <!-- /.content -->
</div>
<!-- /.content-wrapper -->

<input type="hidden" id="messageType" value="@_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyMessageType)" />
<input type="hidden" id="message" value="@_httpContextAccessor.HttpContext.Session.GetString(SessionVariable.SessionKeyMessage)" />

@section Scripts{
    <script type="text/javascript">

        $(function () {

            var messageTypeItem = document.getElementById("messageType");
            var messageItem = document.getElementById("message");

            if (messageTypeItem.value == "success")
                toastr.success(messageItem.value, "Success Message");
            else if (messageTypeItem.value == "error")
                toastr.error(messageItem.value, "Error Message");
            else if (messageTypeItem.value == "info")
                toastr.info(messageItem.value, "Info Message");
            else if (messageTypeItem.value == "warning")
                toastr.warning(messageItem.value, "Warning Message");
        });
    </script>
}